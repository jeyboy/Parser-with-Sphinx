.flow_button
  =link_to image_tag('images.jpeg'), news_category_topic_path(:category_id => @curr_category, :id => @curr_topic)

%p.title= @news_item.title
%br/
%p= raw @news_item.body
%div.bottom_link
  %p= link_to "Назад", news_category_topic_path(:category_id => @curr_category, :id => @curr_topic)